{ 
    "version": 2,
    "builds": [
        {"src": "/dist/server.js",
        "config": {
            "includeFiles": ["dist/**"]}
        }
    ],
    "routes": [{ "src": "/(.*)", "dest": "/dist/server.js" }] 
}
